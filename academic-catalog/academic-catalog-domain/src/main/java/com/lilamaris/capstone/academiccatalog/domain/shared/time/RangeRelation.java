package com.lilamaris.capstone.academiccatalog.domain.shared.time;

public enum RangeRelation {
    SAME,
    CONTAINS,
    CONTAINED_BY,
    BEFORE,
    AFTER,
    OVERLAPS_AFTER,
    OVERLAPS_BEFORE
}
